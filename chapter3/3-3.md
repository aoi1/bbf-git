mkdir study-3-3
cd study-3-3
git init

// 3.3.1
git add
git add .
git add <file name>
echo "hello" >> test.txt
git add test.txt

// 3.3.2
git status

// 3.3.3
git commit -m "コミットコメントです"
git commit --allow-empty
git commit --amend
git commit -m "This is a comet message"
git commit --amend
git log

// 3.3.4
echo "This is file1." >> file1.txt
echo "This is file2." >> file2.txt
git add file1.txt file2.txt
git commit -m "Add file1 and file2"
echo "New line in file1." >> file1.txt
echo "New line in file2." >> file2.txt
git diff
git diff -- file1.txt
git add file1.txt
git diff --staged
git commit -m "Add new line in file1"
git log
git diff <Newest Commit ID>
git diff <Second Newest Commit ID> <Newest Commit ID>

// 3.3.5
git log

git log --abbrev-commit

// 3.3.6
git show <First Commit ID>

