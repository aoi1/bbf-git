// 4.3.1
mkdir study-4-3-break-branch
cd study-4-3-break-branch
git init

// 4.3.2
echo "Initial UI file" >> ui.txt
git add ui.txt
git commit -m "Initial commit on main. Add UI file"

git switch -c feature/new-ui
echo "Feature branch change" >> ui.txt
git add ui.txt
git commit -m "Change ui.txt on feature branch"

// 4.3.3
git switch main
echo "Fix bug in UI" >> ui.txt
git add ui.txt
git commit -m "Change ui.txt on main"

// 4.3.4
git switch feature/new-ui

git rebase main

cat ui.txt

// 4.3.5
git switch main

git add ui.txt
git commit -m "Fix conflict"

git rebase --continue
