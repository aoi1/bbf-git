mkdir study-4-1-branch
cd study-4-1-branch
git init

// 4.1.1
git branch

echo "This is file1" >> file1.txt
echo "This is file2" >> file2.txt
echo "This repository is study-4-1-branch" >> README.md

git add .
git commit -m "First Commit"

git branch

git branch testing

git branch

git switch testing

git branch

git log

echo "This is file3" >> file3.txt
git add file3.txt
git commit -m "Add file3.txt"

git log

git diff testing main

git switch main

// 4.1.2
git branch

git branch -d testing

git branch -D testing

