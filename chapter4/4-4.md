// 4.4.1
mkdir study-4-4
cd study-4-4
git init

echo "test" >> test.txt

git status

echo "test.txt" >> .gitignore

git status

// 4.4.2
echo "git tag sample" >> sample.txt
git add sample.txt
git commit -m "Add sample.txt"

git tag v1.0

git tag

// 4.4.3
git worktree add ../hotfix

git worktree list
