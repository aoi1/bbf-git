// 4.2.2
mkdir study-4-2-2-stash
cd study-4-2-2-stash
git init
echo "This is file.txt" >> file.txt
git add file.txt
git commit -m "Initial commit"

cat file.txt

git switch -c feature
echo "Feature branch update" >> file.txt
git add file.txt
git commit -m "Update file.txt on feature branch"

cat file.txt

echo "Update again" >> file.txt

cat file.txt

git diff HEAD

git switch main

git stash

cat file.txt

git switch main

git switch feature

cat

git status

git stash pop

cat file.txt

// 4.2.3
mkdir study-4-2-3-rebase
cd study-4-2-3-rebase
git init

git switch -c feature

touch feature.txt

git add feature.txt
git commit -m "Fix"

echo "おはよ～お" >> feature.txt

git add feature.txt
git commit -m "Fix"

echo "Good Morning" >> feature.txt
git add feature.txt
git commit -m "Fix"

git log

git rebase -i HEAD~2

git log

// 4.2.4
mkdir study-4-2-4-detached-head
cd study-4-2-4-detached-head
git init
echo "v1" >> file.txt
git add file.txt
git commit -m "first commit"

echo "v2" >> file.txt
git commit -am "second commit"

git log --oneline

git switch --detach <first commit Commit ID>

echo "v1.1" >> file.txt
git commit -am "detached commit"

git log --oneline

git switch main

git log --oneline
