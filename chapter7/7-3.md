// 7.3.1
mkdir study-7-3
cd study-7-3
git init
echo "# Chapter7-3" >> README.md
git add README.md
git commit -m "chore: initial commit"
gh repo create study-7-3 --private --source=. --remote=origin --push

git branch newbranch1
git branch newbranch2

// 7.3.2
git switch newbranch1

git add README.md
git commit -m "Add hi in README.md"
git push -u origin newbranch1

gh pr create --title "READMEを直しました" --body "Chapter7のハンズオンを行いました"

gh pr merge 1 --merge

// 7.3.3
git switch newbranch2

cat README.md

git add README.md
git commit -m "README.mdに挨拶を追加しました"

git push -u origin newbranch2

gh pr create --title "README.mdに挨拶を追加しました" --body "Chapter7のハンズオンを行いました"


