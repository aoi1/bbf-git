// 7.2.1
mkdir study-7-2
cd study-7-2
git init
echo "# study-7-2" >> README.md
git add README.md
git commit -m "first commit"
gh repo create study-7-2 --private --source=. --remote=origin --push

// 7.2.2
git switch -c conflict-branch

echo "Hello" >> hello.txt

git add hello.txt
git commit -m "feat: add hello.txt"
git push -u origin conflict-branch

gh pr create --title "hello.txtを足しました" --body "Chapter7のハンズオンを行いました"

// 7.2.4
echo "Local change" >> hello.txt

git add hello.txt
git commit -m "Local change (not pushed)"

git pull

git config pull.rebase false

git pull

// 7.2.5
cat hello.txt

git add hello.txt
git commit -m "コンフリクトを直しました"

git pull


