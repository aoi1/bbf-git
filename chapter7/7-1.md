// 7.1.1
mkdir study-7-1
cd study-7-1
git init

echo "# Chapter7-1 repository" >> README.md
git add .
git commit -m "first commit"

gh repo create study-7-1 --private --source=. --remote=origin --push

// 7.1.2
echo "テスト用ファイルだよ～ん" >> test1.txt

echo "大事なファイル" >> file.txt

git status

git add .

git status

git restore --staged test1.txt

git status

// 7.1.3
git add test1.txt
git commit -m "本番用ファイルを追加"

git log

git show

// 7.1.4
git reset --soft <Commit ID>

git status

git commit -m "本番用ファイルを追加"

git reset --mixed <Commit ID>

git status

git add .
git commit -m "本番用ファイルを追加"

git reset --hard HEAD~1

git status

ls

// 7.1.5
git reset --hard <Commit ID>

ls

// 7.1.6
git switch -c 7-1-6

git push -u origin 7-1-6

git revert HEAD

git log

git push


