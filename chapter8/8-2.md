// 8.2.1
mkdir study-8-2-revert
cd study-8-2-revert
git init
echo "# study-8-2-revert" >> README.md
git add README.md
git commit -m "first commit"
gh repo create study-8-2-revert --private --source=. --remote=origin --push

chmod +x hello.sh

./hello.sh

git add hello.sh
git commit -m "Add hello.sh"
git push

// 8.2.2
git switch -c add-line

echo '$(undefined_command)' >> hello.sh

./hello.sh

// 8.2.3
git add hello.sh
git commit -m "undefined_commandを実行する行を追加しました"
git push -u origin add-line

gh pr create --title "undefined_commandを追加" --body "undefined_commandを実行する行を追加しました"



